<div id="change-password" class="modal hide fade modal-register" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
  	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h2>Contraseña</h2>
  </div>
	<div class="modal-body">
		<%= simple_form_for(current_user, :url => { :action => "update_password", :controller => "users" }) do |f| %>
		<%= f.error_notification %>
		<%= display_base_errors resource %>
		<%= f.password_field :password, :autofocus => true, :label => false, :placeholder => 'Nueva contraseña' %>
		<%= f.password_field :password_confirmation, :label => false, :placeholder => 'Ingresala nuevamente' %>
	</div>
	<div class="modal-footer">
		  <%= f.button :submit, "Cambiar", :class => 'btn-primary' %>
		<% end %>
		<a href="#" class="btn" data-dismiss="modal">Cerrar</a>
	</div>
</div>
