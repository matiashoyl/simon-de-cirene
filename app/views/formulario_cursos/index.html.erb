<h1>Listing formulario_cursos</h1>

<table>
  <tr>
    <th>Formulario</th>
    <th>Curso</th>
    <th>Estado</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @formulario_cursos.each do |formulario_curso| %>
  <tr>
    <td><%= formulario_curso.formulario_id %></td>
    <td><%= formulario_curso.curso_id %></td>
    <td><%= formulario_curso.estado %></td>
    <td><%= link_to 'Show', formulario_curso %></td>
    <td><%= link_to 'Edit', edit_formulario_curso_path(formulario_curso) %></td>
    <td><%= link_to 'Destroy', formulario_curso, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Formulario curso', new_formulario_curso_path %>
