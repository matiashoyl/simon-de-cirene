<%= simple_form_for(@mensaje_usuario) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :mensaje_id %>
    <%= f.input :usuario_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
