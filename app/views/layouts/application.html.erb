<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Simon De Cirene" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Simon De Cirene" %>">
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%# application layout styled for Twitter Bootstrap 2.3 %>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <div class="container">
        <div class="content">
           <div class="row-fluid">
            <div class="span12">
              <%= yield %>
            </div>
          </div>
          <footer>
          </footer>
        </div>
      </div>
    </main>
    <%= render 'devise/registrations/new' %>
    <%= render 'devise/sessions/new' %>
    <% if user_signed_in? %>
      <%= render 'users/change_password' %>
    <% end %>
  </body>
  
</html>
