<div class="span2"></div>
<div class="span8">
  <h3>Formularios</h3>
  <% if @formularios.any? %>
    <table class="table table-condensed table-striped">
      <thead>
        <tr>
          <th>Código</th>
          <th>Nombre</th>
          <th>Tipo</th>
        </tr>
      </thead>
      <tbody>
        <% @formularios.each do |formulario| %>
          <tr>
            <td><%= formulario.codigo %></td>
            <td><%= formulario.nombre %></td>
            <td><%= formulario.tipo %></td>
            <td>
              <div class="btn-group">
                <%= link_to("Ver", formulario, :class => 'btn btn-mini') %>
                <%= link_to("Editar", edit_formulario_path(formulario), :class => 'btn btn-mini') %>
                <a data-toggle="modal" href="#delete-formulario-<%= formulario.id %>" class="btn btn-mini btn-inverse" type="button">Eliminar</a>
              </div>
              <%= render :partial => 'eliminar', :locals => {:formulario => formulario} %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <br />
  <% end %>
  <%= link_to("Nuevo Formulario", new_formulario_path, :class => 'btn btn-mini btn-info') %>
</div>